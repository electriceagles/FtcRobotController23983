/**
 * Top-level build file for ftc_app project.
 */

buildscript {
    repositories {
        mavenCentral()
        google()
        maven { url = 'https://maven.brott.dev/' }
    }
    dependencies {
        // FTC teams should not modify this
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
        maven { url = 'https://maven.brott.dev/' }
        maven {
            url = "https://mymaven.bylazar.com/releases"
        }
    }
}